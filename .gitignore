# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# venv / uv
.venv/

# 初回起動時に生成されるカード画像（生成物のため追跡しない）
src/lUNO/static/cards/

# デプロイ設定は環境固有（絶対パス・ユーザ名・同居する他サービス構成等）を含むため
# リポジトリでは追跡しない。実体は各環境のローカルで管理する。
deploy/

# OS
.DS_Store
